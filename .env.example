# Vultr Managed PostgreSQL Connection Configuration
# 
# SETUP INSTRUCTIONS:
# 1. Copy this file and save it as .env
#    Example: cp .env.example .env
# 
# 2. Get your connection credentials from Vultr Customer Portal:
#    - Go to: Databases → Your PostgreSQL Cluster → Overview tab
#    - Copy the connection details
# 
# 3. Replace the placeholder values below with your actual credentials

# Database Host
DB_HOST=your-vultr-postgresql-host.vultr.com

# Database Port
DB_PORT=18140

# Database Username (default is usually vultradmin)
DB_USER=vultradmin

# Database Password
DB_PASSWORD=your-password-here

# Database Name (default is usually defaultdb)
DB_NAME=defaultdb